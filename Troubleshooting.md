# 🛠️ Troubleshooting - Решение частых проблем

## 🔧 Проблемы с установкой

### "Не могу найти пакет"
```bash
# Обновить список пакетов
sudo apt update

# Или поискать по названию
apt search имя_пакета


"Не хватает прав"
# Добавить пользователя в группу sudo (если ещё не добавлен)
sudo usermod -aG sudo имя_пользователя


Проблемы с графикой
"Программа запускается, но тормозит"
Проверить свободную память: gnome-system-monitor, закрыть лишние приложения, перезапустить программу

"Шрифты выглядят странно"
# Обновить кэш шрифтов
sudo fc-cache -f -v

Проблемы с файлами
"Не вижу файлы на флешке"
Открыть Nemo → в боковом меню выбрать флешку

Или найти в папке /media/имя_пользователя/


"Не открывается PSD файл"
Использовать GIMP (поддержка PSD)

Или установить дополнительно: sudo apt install gimp-plugin-registry

Интернет и сети
"Не работает интернет"
# Проверить подключение
ping google.com

# Перезапустить сетевой менеджер
sudo systemctl restart NetworkManager


Место на диске
"Заканчивается место"
# Посмотреть размер папок
du -sh ~/Загрузки/ ~/.cache/

# Очистить кэш пакетов
sudo apt clean
