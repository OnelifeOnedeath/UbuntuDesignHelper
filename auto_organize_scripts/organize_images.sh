#!/bin/bash
# 📸 Автоматическая организация изображений по датам

echo "🔄 Организую изображения..."

# Создать папки по годам и месяцам
for file in *.jpg *.jpeg *.png *.psd; do
    if [ -f "$file" ]; then
        date=$(stat -c %y "$file" | cut -d' ' -f1)
        year=$(echo $date | cut -d'-' -f1)
        month=$(echo $date | cut -d'-' -f2)
        
        mkdir -p "$year/$month"
        mv "$file" "$year/$month/"
        echo "📁 Перемещён: $file → $year/$month/"
    fi
done

echo "✅ Готово! Изображения организованы по датам"
